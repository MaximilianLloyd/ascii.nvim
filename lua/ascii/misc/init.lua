local skulls = require("ascii.misc.skulls")
local krakens = require("ascii.misc.krakens")
local hydra = require("ascii.misc.hydra")
local sunset = require("ascii.misc.sunset")

local M = {
	skulls = skulls,
	krakens = krakens,
	hydra = hydra,
	sunset = sunset,
}

return M
